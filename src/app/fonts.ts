import { Inter } from 'next/font/google'

export const headingFont = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const bodyFont = Inter({
    subsets: ['latin'],
    display: 'swap',
})