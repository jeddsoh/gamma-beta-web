import { Figtree } from 'next/font/google'

export const headingFont = Figtree({
    subsets: ['latin'],
    display: 'swap',
})

export const bodyFont = Figtree({
    subsets: ['latin'],
    display: 'swap',
})